# Test environment variables for development
# Note: In production, use your actual SMTP credentials

# For testing with a simple service like Gmail (requires app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=test@example.com
SMTP_PASS=test-password

# Email recipients
MAIL_FROM=web@cityexperts.ae
MAIL_TO=admin@cityexperts.ae

# reCAPTCHA (optional for testing)
# VITE_RECAPTCHA_SITE_KEY=your-site-key
# RECAPTCHA_SECRET=your-secret-key