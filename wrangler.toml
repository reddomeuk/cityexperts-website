name = "cityexperts-website"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# Pages configuration - minimal and clean
pages_build_output_dir = "dist"

# D1 Database Configuration (disabled until database is created)
# [[d1_databases]]
# binding = "DB"
# database_name = "city-experts-db"
# database_id = "your-database-id-here" # Get this from: npx wrangler d1 create city-experts-db

# Environment variables
[vars]
ENVIRONMENT = "development"
CONTACT_EMAIL = "enquiry@cityexpertsdubai.com"

# Note: Cloudinary variables are set in Pages dashboard:
# CLOUDINARY_CLOUD_NAME = "dmawj7tmu" (set in dashboard)
# CLOUDINARY_API_KEY = "your-api-key" (encrypted in dashboard)
# CLOUDINARY_API_SECRET = "your-api-secret" (encrypted in dashboard)