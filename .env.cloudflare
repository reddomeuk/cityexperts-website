# Environment Variables for Cloudflare Pages

# Application Settings
ENVIRONMENT=production
CONTACT_EMAIL=info@cityexperts.ae
FROM_EMAIL=noreply@cityexperts.ae

# Email Service Configuration (Choose ONE)

# Option 1: SendGrid (Recommended)
# SENDGRID_API_KEY=SG.your_sendgrid_api_key_here

# Option 2: Mailgun
# MAILGUN_API_KEY=your_mailgun_api_key_here
# MAILGUN_DOMAIN=your_mailgun_domain.com

# Option 3: EmailJS (Client-side service)
# EMAILJS_SERVICE_ID=your_emailjs_service_id
# EMAILJS_TEMPLATE_ID=your_emailjs_template_id
# EMAILJS_PUBLIC_KEY=your_emailjs_public_key

# Optional: reCAPTCHA v3 for form protection
# RECAPTCHA_SECRET=your_recaptcha_secret_key

# Optional: Analytics endpoint for tracking
# ANALYTICS_ENDPOINT=https://your-analytics-service.com/track

# Optional: Custom domain settings
# DOMAIN=cityexperts.ae
# SUBDOMAIN=www

# Database Configuration (if using D1)
# DATABASE_ID=your_d1_database_id

# Storage Configuration (if using R2)
# R2_BUCKET_NAME=cityexperts-uploads

# KV Storage Configuration
# PROJECTS_KV_ID=your_projects_kv_namespace_id
# ANALYTICS_KV_ID=your_analytics_kv_namespace_id